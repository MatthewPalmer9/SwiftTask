<nav class="sticky" id="navbar">
    <div class="navbar-container">
    <ul class="navbar-ul">

        <li class="nav-brand"><a href="/"> <span id="slashes">//</span> SWIFT TASK </a></li>

        <li class="list-item"><a class="nav-link" href="/"> HOME </a></li>
        <li class="list-item"><a class="nav-link" href="/about"> ABOUT </a></li>
        <li class="list-item"><a class="nav-link" href="/dashboard"> DASHBOARD </a></li>
        <li class="list-item"><a class="nav-link" href="/projects"> PROJECTS </a></li>
        <li class="list-item"><a class="nav-link" href="/tasks"> TASKS </a></li>

        <% if current_user.nil? %>
            <li class="list-right"><a class="login" href="/login">LOG IN</a></li>
            <li class="list-right"><a class="signup" href="/signup">SIGN UP</a></li>
        <% elsif current_user.provider == "facebook" %>
            <li class="list-right greeting"> <img class="fb-img" src="<%= current_user.image %>"> Hello, <%= current_user.name %>!
              <%= form_for(:logout, url: '/logout') do |f| %>
                <%= f.submit "Log Out >>", :class => 'btn' %>
                <% end %>
            </li>
        <% else %>
            <li class="list-right greeting">Hello, 
            <% if current_user.provider.nil? %>
                <%= current_user.first_name %>
            <% else %>
                <%= current_user.name %>
            <% end %>!
              <%= form_for(:logout, url: '/logout') do |f| %>
                <%= f.submit "Log Out >>", :class => 'btn' %>
                <% end %>
            </li>
        <% end %>
    </ul>
    </div>
</nav>